apiVersion: v1
kind: Service
metadata:
  name: {{ include "fizzy.fullname" . }}
  labels:
    {{- include "fizzy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "fizzy.selectorLabels" . | nindent 4 }}
